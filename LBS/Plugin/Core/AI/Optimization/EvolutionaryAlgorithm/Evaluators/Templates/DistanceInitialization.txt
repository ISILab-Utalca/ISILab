
            List<int> elements = #FIRST_TAGCHAR#;

            int size = elements.Count;

            if(size < 2)
            {
                Debug.LogWarning("Not enough elements to perform pathfinding.");
                //return 0.0f;
            }

            float[,] distances = new float[size, size];
