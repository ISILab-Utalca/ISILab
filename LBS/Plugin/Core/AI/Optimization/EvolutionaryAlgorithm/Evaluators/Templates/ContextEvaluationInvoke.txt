
            LBSLayer layer = CombinedLayer;
            if (layer is not null)
            {
                fitness = EvaluateWithContext(null, chrom, null);
            }
            else
            {
                fitness = EvaluateWithoutContext(null, chrom);
            }
